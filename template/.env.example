VITE_BACKEND_URL=https://citeopolis-v5.fr.dev08.stratis.pro
VITE_GRAPHQL_URL=$VITE_BACKEND_URL/wp/graphql

# Schema source for graphql-codegen.
# Option A (recommended): local schema file from the installed @citeopolis-graphql/schema package
INTROSPECTION_URL=./node_modules/@citeopolis-graphql/schema/dist/schema.graphql
# Option B: live introspection from the GraphQL endpoint (requires the API to be reachable)
# INTROSPECTION_URL=$VITE_GRAPHQL_URL

# Set to "true" to use mock GraphQL data (no real API needed — for demos and CI)
VITE_MOCK_API=false

STORYBOOK_DISABLE_TELEMETRY=true
